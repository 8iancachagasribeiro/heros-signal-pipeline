# Exact versions used to produce every number in the manuscript.
# Python 3.12.3
numpy==2.4.4
scipy==1.17.1
pandas==3.0.2
matplotlib==3.10.8
statsmodels==0.14.6      # used ONLY to validate fastlrt.py; not needed to reproduce results
